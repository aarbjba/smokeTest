# Backend
API_PORT=3001
# 32 bytes hex for AES-256-GCM token encryption (generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))")
ENCRYPTION_KEY=change-me-please-generate-a-proper-32-byte-hex-key-1234567890abcdef
# SQLite file (relative to apps/api)
DB_PATH=./data/werkbank.db
